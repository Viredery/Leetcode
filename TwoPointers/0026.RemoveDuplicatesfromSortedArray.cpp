class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        if (nums.empty())
            return 0;
        int pre = -1, cur = 0, next = 0;
        while (cur < nums.size()) {
            while(++next < nums.size() && nums[next] == nums[cur]) ;
            nums[++pre] = nums[cur];
            cur = next;
        }
        return pre+1;
    }
};
